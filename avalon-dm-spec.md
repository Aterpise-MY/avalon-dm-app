# 阿瓦隆 DM 助手 — 完整设计文档

> 一个给主持人（DM）用的单机 App：手机传一圈拍照建档，发身份、跑夜晚、管任务、判刺杀。
> 全程只有一台设备，不联网、不开房间、不注册。

- 版本：v1.0
- 载体：React 单文件组件（Claude Artifact），移动端优先
- 实现文件：`avalon-dm.jsx`

---

## 1. 这个 App 解决什么

线下玩阿瓦隆，主持人的负担几乎全在记忆和口播上：

| 痛点 | App 的处理 |
|---|---|
| 记不住谁是谁，尤其是新朋友局 | 开局传一圈拍照，之后所有界面都用真人头像 |
| 洗牌发牌慢，还容易被瞄到 | 手机传一圈，火漆封信，各自拆封看身份 |
| 夜晚口播容易漏、容易念错顺序 | 逐句脚本，每一步显示"此刻该动作的人"的头像，抬头就能核对 |
| 队长轮转、连续否决、第四轮双失败牌容易忘 | 全部自动，界面上直接标出来 |
| 复盘时想不起哪轮谁投了反对 | 逐人记录投票，终局回顾 |

**明确不做的事**：不联机、不做多设备、不做战绩排行榜、不替代实体任务牌（成功/失败牌仍然用实体牌，App 只收结果）。

---

## 2. 游戏规则基线

采用《The Resistance: Avalon》标准规则。

### 2.1 阵营人数

| 总人数 | 好人 | 坏人 |
|---|---|---|
| 5 | 3 | 2 |
| 6 | 4 | 2 |
| 7 | 4 | 3 |
| 8 | 5 | 3 |
| 9 | 6 | 3 |
| 10 | 6 | 4 |

### 2.2 每轮出队人数

| 总人数 | 第1轮 | 第2轮 | 第3轮 | 第4轮 | 第5轮 |
|---|---|---|---|---|---|
| 5 | 2 | 3 | 2 | 3 | 3 |
| 6 | 2 | 3 | 4 | 3 | 4 |
| 7 | 2 | 3 | 3 | 4 | 4 |
| 8 | 3 | 4 | 4 | 5 | 5 |
| 9 | 3 | 4 | 4 | 5 | 5 |
| 10 | 3 | 4 | 4 | 5 | 5 |

**双失败牌规则**：7 人及以上时，第 4 轮任务需要 **2 张**失败牌才算失败；其余所有情况 1 张即失败。界面上该轮用 `✦` 标记。

### 2.3 角色

| 角色 | 阵营 | 能力 |
|---|---|---|
| 梅林 Merlin | 好 | 知晓所有坏人（莫德雷德除外） |
| 派西维尔 Percival | 好 | 看到梅林与莫甘娜两人，但分不清谁是谁 |
| 忠臣 Loyal Servant | 好 | 无情报 |
| 刺客 Assassin | 坏 | 好人三胜后，由他指认梅林 |
| 莫甘娜 Morgana | 坏 | 在派西维尔眼中与梅林无法区分 |
| 莫德雷德 Mordred | 坏 | 梅林看不见他 |
| 奥伯伦 Oberon | 坏 | 不认识其他坏人，其他坏人也不认识他 |
| 爪牙 Minion | 坏 | 无情报，填充剩余坏人位 |

**牌堆生成逻辑**

- 坏人：刺客必选 → 依次加入勾选的莫甘娜 / 莫德雷德 / 奥伯伦 → 截断到本局坏人位数 → 不足部分用爪牙补齐
- 好人：梅林必选 → 勾选则加入派西维尔 → 剩余全部为忠臣
- 若勾选的特殊坏人超出坏人位数量，界面提示"多选的会被忽略"，按上面顺序截断

### 2.4 胜负判定

- 三次任务成功 → 进入刺杀阶段
  - 刺客指认正确（目标是梅林）→ 坏人获胜
  - 指认错误 → 好人获胜
- 三次任务失败 → 坏人获胜
- 同一轮内连续 5 次组队被否决 → 坏人获胜（否决计数在任务执行后清零）

---

## 3. 主流程

```
入座 → 配角色 → [App 发牌 → 传阅身份] 或 [实体牌 → 夜晚逐角色确认]
     → 夜晚仪式（可跳过） → 任务循环 ×N → 刺杀 → 终局亮牌
```

### 3.1 入座

1. 选人数（5–10），下方即时显示"X 位好人 · Y 位坏人"
2. 生成对应数量的空位
3. 手机传一圈：每人点自己的头像圈 → 调起系统相机自拍 → 填名字
4. 头像可跳过，跳过时显示名字首字
5. 全员填完名字后可进入下一步

**名单持久化**：进入下一步时通过 Supabase RPC 把 `{id, name, photo}` 写入 PostgreSQL，并保存座位快照。下次开局读取当前 DM 账号最近一次名单。Supabase 不可用时回退到 `window.storage` 或 `localStorage`，且不阻断流程。

**照片处理**：读入后用 canvas 居中裁成正方形、缩放到 260×260、JPEG 质量 0.72 导出 dataURL。10 人的名单大约 100–300 KB，远低于单键 5 MB 上限。

### 3.2 配角色

- 四个开关：派西维尔 / 莫甘娜 / 莫德雷德 / 奥伯伦，各自带一行说明
- 实时预览本局牌堆（角色标签按阵营着色）
- 选择分身份方式：
  - **App 发牌** — 手机传一圈，各自拆封
  - **实体牌** — 抽完牌后，DM 在夜晚流程中按角色揭晓顺序逐个确认并点选玩家

### 3.3 传阅身份（App 发牌）

每人一屏，分两态：

**封口态** — 大头像 + 名字 + 一枚火漆印。提示"确认手机在你自己手上，再按下火漆印"。

**拆封态** — 羊皮纸质感的身份卡：
- 角色名（按阵营着色）
- 能力说明
- 该角色能看到的人（头像 + 名字）

各角色看到的内容：

| 角色 | 标题 | 名单 |
|---|---|---|
| 梅林 | 以下是邪恶阵营 | 所有坏人，排除莫德雷德（奥伯伦可见） |
| 派西维尔 | 这两人之一是梅林 | 梅林 + 莫甘娜，**随机打乱顺序** |
| 坏人（非奥伯伦） | 你的同伙 | 其他坏人，排除奥伯伦 |
| 奥伯伦 / 忠臣 | — | 空，显示"你今晚看不到任何人" |

看完点「记住了，传给下一位」，自动回到封口态并切换到下一人。最后一人点「全部看完，进入夜晚」。

> 派西维尔的名单每次进入都会重新洗牌，避免"第一个总是梅林"被摸出规律。

### 3.4 夜晚仪式

逐句脚本，每屏一步，下方显示此刻该有动作的人的头像供 DM 核对。

使用实体牌时，不再在夜晚前一次性录入全员身份。DM 在角色睁眼时直接点选对应玩家；未启用的角色自动跳过，最后未被点选的好人自动记为忠臣。确认顺序为：

1. 奥伯伦（如有）
2. 莫甘娜（如有）
3. 刺客
4. 其他邪恶角色（莫德雷德 / 爪牙，如有）
5. 邪恶阵营相认
6. 梅林
7. 梅林确认坏人
8. 派西维尔（如有）
9. 派西维尔确认梅林与莫甘娜
10. 天亮

进入任务后，页面顶部按同一顺序显示“角色 → 玩家”的夜晚确认名单。

1. **闭眼** — 所有人闭眼，握拳，伸出大拇指
2. **坏人相认** — 邪恶阵营睁眼互相确认（有奥伯伦时补一句"奥伯伦不睁眼，也不被看到"）
3. **梅林确认坏人** — 所有坏人（有莫德雷德时排除）伸拇指，梅林睁眼记人
4. **派西维尔确认** — 梅林与莫甘娜伸拇指，派西维尔睁眼（仅在选了派西维尔时出现）
5. **天亮** — 所有人睁眼

用 App 发牌时，情报已在拆封环节给过，第一屏提供「大家已在手机上看过情报，跳过夜晚」。用实体牌时按完整脚本走。

### 3.5 任务循环

顶部常驻状态条：

- 五个圆点代表五轮，内含该轮出队人数，`✦` 标记双失败牌轮次；已完成的轮按结果染成蓝（成功）/ 红（失败）
- 连续否决进度：5 个小点

每一轮以六个明确环节引导 DM，并在页面上显示「DM 现在要做」的口播或操作提示：

1. **交队长** — 把实体队长标记交给当前队长，并宣布本轮人数
2. **组队** — 队长提名，DM 在 App 中点选任务成员
3. **投票** — 全员同时亮票，DM 逐个记录
4. **票决** — 单独显示并宣布通过 / 否决结果，宣布后才进入下一环节
5. **任务** — 收齐任务牌、洗混并录入失败牌数
6. **结果** — 单独显示并宣布成功 / 失败，宣布后才进入下一轮、刺杀或终局

**组队** — 显示当前队长（金圈头像）。点头像加入/移出队伍，凑满人数后进入投票。

**投票** — 逐个点头像切换状态：未投 → 赞成 → 反对。全员投完后按钮显示比分。
- 赞成 > 反对 → 进入任务结算
- 否则 → 否决数 +1，队长顺延，回到组队；否决数达 5 直接判坏人胜

**任务结算** — 实体失败牌收上来后，点数字按钮录入失败牌数量。界面明示本轮需要几张才算失败。录入后：
- 否决数清零、队长顺延
- 累计 3 次失败 → 坏人胜
- 累计 3 次成功 → 刺杀阶段
- 否则进入下一轮

**队长起始**：开局随机选一位，之后按名单顺序循环。

### 3.6 刺杀

显示刺客是谁（提醒 DM 请他表态），列出所有好人阵营玩家的头像，点谁就是指认谁。

### 3.7 终局

- 大标题写明哪方获胜，副标题写明原因（刺客扑空 / 指中梅林 / 三次任务失败 / 五次否决）
- 全员亮牌，被刺杀者带标记
- 任务回顾：每轮成功失败、失败牌数、当轮队长
- 两个出口：「同一批人再来」（回配角色页）/「换一局」（回入座页）

### 3.8 DM 底牌

除传阅身份阶段外，右上角常驻「按住看底牌」。按住显示全场身份，松手立即消失——避免误触后停留在屏幕上被人看到。

---

## 4. 数据模型

```js
Player = {
  id: string,        // 稳定唯一 id
  name: string,
  photo: string|null, // dataURL，260×260 JPEG
  role: string|null,  // ROLES 的键
}

State = {
  phase: 'setup'|'roles'|'pass'|'night'|'game'|'assassin'|'end',
  count: 5..10,
  players: Player[],
  opts: { percival, morgana, mordred, oberon },  // 布尔
  dealMode: 'auto'|'manual',

  passIdx, sealBroken,      // 传阅
  nightStep,                // 夜晚步骤（实体牌模式同时承担逐角色点选确认）

  round: 0..4,
  step: 'leader'|'team'|'vote'|'voteResult'|'mission'|'missionResult',
  leader: number,           // players 下标
  team: string[],           // player id
  votes: { [id]: true|false|undefined },
  rejects: 0..5,
  history: [{ fails, ok, team, leader }],
  missionResult: { fails, ok }|null,
  gameId: string|null,
  dbStatus: 'checking'|'ready'|'saving'|'saved'|'offline',
  winner: 'good'|'evil-assassin'|'evil-mission'|'evil-vote'|null,
  killed: Player|null,
}
```

**持久化**：Supabase PostgreSQL 保存玩家与座位快照、对局配置和身份、每次组队投票、每轮任务结果、最终胜方与刺杀目标。页面顶部显示保存状态。云端离线时游戏仍可继续，但完整对局历史只保留在当前内存；浏览器回退存储只保存玩家名单，不保存秘密身份。所有业务表使用 RLS 按 DM 的 `auth.uid()` 隔离。

---

## 5. 视觉规范

主题取自亚瑟王宫廷：烛光下的深靛底色、金箔线条、羊皮纸身份卡。

| 名称 | 色值 | 用途 |
|---|---|---|
| ink | `#10101E` | 页面底色 |
| ink2 | `#171730` | 底栏 |
| panel | `#1C1C38` | 卡片、输入框 |
| line | `#2E2E52` | 分隔线、未选中描边 |
| gold | `#C8A24C` | 主按钮、队长、选中态 |
| goldDim | `#8A7038` | 眉标、次级金 |
| vellum | `#E9E2D0` | 身份卡纸面 |
| azure | `#4C7BD9` | 好人阵营、任务成功 |
| crimson | `#A8323A` | 坏人阵营、任务失败、火漆 |
| text | `#E4E2EE` | 正文 |
| dim | `#8483A6` | 辅助文字 |

**字体**
- 展示体：Georgia / Times New Roman 衬线，用于标题、角色名、脚本正文，字距放宽
- 正文：系统默认无衬线
- 数据：等宽字体，用于人数、轮次、失败牌数

**签名元素**：火漆印。拆封这一下把"传手机"从一个尴尬的操作变成一个仪式动作，也是整个 App 唯一一处高饱和红。其余界面保持克制——平面色块、金色发丝线、无渐变。

**交互约定**
- 所有可点元素按下缩放 95%
- 头像描边颜色即状态：金=选中/队长，蓝=好人/赞成，红=坏人/反对，灰=未选
- 触控目标不小于 44pt，头像网格三列

---

## 6. 边界情况处理

| 情况 | 处理 |
|---|---|
| 相机权限被拒 | 头像回退为名字首字，不阻断流程 |
| 未填名字 | 底部按钮禁用，文案改为"还有人没填名字" |
| 中途改人数 | 已填的玩家保留，多出的位补空、少的截断 |
| 特殊坏人勾多了 | 红字提示，按刺客→莫甘娜→莫德雷德→奥伯伦顺序截断 |
| 夜晚确认重复选择玩家 | 已确认其他身份的玩家置灰，可返回上一步更正 |
| Supabase 不可用 | 显示「云端离线」，游戏继续，名单回退浏览器存储 |
| 第二局复用名单 | 进入配角色页时清空所有 role，夜晚步骤归零 |

---

## 7. 已知取舍

- **DM 单账号**。玩家不需要账号；主持人使用 Email/password 登录后可从不同设备读取自己的数据。
- **不代管任务牌**。成功/失败牌仍用实体牌，App 只录入结果——保留出牌的手感和"谁出的失败牌"的悬念。
- **不做撤销**。误点投票可以重新切换；已提交的任务结果无法回退。真出错了只能靠 DM 口头澄清。
- **照片写入 Supabase**。上传前应取得玩家同意；前端只使用 publishable key，不包含 service-role key。

---

## 8. 后续可以加的

优先级从高到低：

1. **座位号 / 发言顺序** — 头像上标 1–10，配合"从队长左手边开始发言"
2. **投票复盘** — 终局展示每轮每人的投票记录，找"连续反对成功队"的可疑者
3. **撤销上一步** — 至少覆盖误录任务结果
4. **湖中仙女** — 扩展角色，需要额外的验人流程屏
5. **计时器** — 组队讨论限时，防止一轮聊二十分钟
6. **本局导出** — 终局生成一张可分享的战报图

---

## 9. 快速上手

给第一次用的 DM：

1. 打开 App，选人数
2. 手机传一圈，每人自拍 + 写名字，传回你手上
3. 勾角色（新手局建议只开派西维尔 + 莫甘娜），选「App 发牌」
4. 手机再传一圈，每人按火漆印看自己身份，看完传下一位
5. 收回手机，跳过夜晚，直接开第一轮
6. 之后照着屏幕点：队长选人 → 逐个记投票 → 收失败牌录数字
7. 好人三胜时 App 会自动叫刺客出手

全程你只需要记住一件事：**手机传出去之前，先确认屏幕停在封口页。**
